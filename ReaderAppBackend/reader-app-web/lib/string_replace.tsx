

export default function string_replace(element_string,search,replace){
    if(typeof(element_string)=="string"){
        if(element_string.includes(search)){
        //   console.log(annotation)
        //   console.log(element_string)
        }
      }
    
}